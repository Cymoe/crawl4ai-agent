[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "streamlit run streamlit_ui.py --server.address=0.0.0.0 --server.port=$PORT"
